// Image export dimensions (timeline PNG export)
export const IMAGE_WIDTH = 1920;
export const IMAGE_HEIGHT = 1080;
export const IMAGE_PADDING = 40;
export const LABEL_WIDTH = 200;
export const HEADER_HEIGHT = 50;
/** Schedule header rows and root item rows. */
export const ROW_HEIGHT = 32;
/** Every item below the root, bar or milestone. */
export const NESTED_ROW_HEIGHT = 24;
export const BAR_HEIGHT = 20;
export const NESTED_BAR_HEIGHT = 16;
/** Nested bars overlap the parent they sit under; the wash keeps both readable. */
export const NESTED_BAR_ALPHA = 0.6;
